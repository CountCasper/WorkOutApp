import java.util.Random;
public class Workout {
	private Layout[] work;
	private int numOfRounds;
	public Workout(){
		Random rand = new Random();
		int  numOfRounds = rand.nextInt(100) + 1;
		this.work = new Layout[numOfRounds];
	}
}
